/**
 * Optional UI package (#40) — components + the react-free helpers they build on. Import from here
 * only when you want the shipped components; the headless hooks/reducers work without them.
 */
export * from "./part-summary.js";
export * from "./components.js";
