{
  "name": "@agentkit/shareflow",
  "version": "0.0.0",
  "private": true,
  "description": "ShareFlow integration: the tools, context providers, skills and agent manifests that make the generic runtime a social assistant. Depends on @agentkit/backend; nothing generic depends on this.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "tsc -b && vitest run",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@agentkit/backend": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
